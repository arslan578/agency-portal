apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: patch-sa.yaml
  - path: env-patch.yaml
  - path: ingress-hosts.yaml
