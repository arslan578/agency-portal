apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: patch-sa.yaml
  - path: env-patch.yaml
  - path: ingress-hosts.yaml
